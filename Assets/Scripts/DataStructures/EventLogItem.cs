public class EventLogItem{
    public string eventText;
    public float eventTime;
    public EventLogItem(string eventText, float eventTime){
        this.eventText = eventText;
        this.eventTime = eventTime;
    }
}